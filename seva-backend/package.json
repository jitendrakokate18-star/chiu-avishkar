{
  "name": "seva-backend",
  "version": "1.0.0",
  "description": "JSON Server Backend for SEVA Health",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
